# Lista Withdraw 快速开始（定期执行版）

## 📋 文件说明

- `lista_withdraw.py` - 主程序，定期执行withdraw操作
- `test_lista_connection.py` - 测试连接和ABI加载
- `LISTA_WITHDRAW_README.md` - 完整使用文档

## 🚀 快速开始

### 1. 测试连接（推荐先执行）

```bash
python test_lista_connection.py
```

这会测试：
- ✅ BSC网络连接
- ✅ 钱包加载
- ✅ ABI文件加载
- ✅ 合约信息获取
- ✅ 余额查询

### 2. 执行定期Withdraw

#### 循环执行模式（推荐）
```bash
# 每60秒检查一次，满足条件时取出0.5个代币
python lista_withdraw.py --amount 0.5 --interval 60

# 每30秒检查一次，满足条件时取出1.0个代币
python lista_withdraw.py --amount 1.0 --interval 30

# 每120秒检查一次，满足条件时取出0.3个代币
python lista_withdraw.py -a 0.3 -i 120
```

#### 单次执行模式
```bash
# 只执行一次检查和取出
python lista_withdraw.py --amount 0.5 --once
```

## 🔧 参数说明

| 参数 | 简写 | 必需 | 默认值 | 说明 |
|------|------|------|--------|------|
| `--amount` | `-a` | ✅ | - | 每次取出的金额 |
| `--interval` | `-i` | ❌ | 60 | 检查间隔（秒） |
| `--once` | - | ❌ | False | 只执行一次 |

## 💡 工作原理

```
循环执行流程：
┌─────────────────────────────────────┐
│  1. 检查最大可取出金额               │
│  2. 比较：最大金额 vs 配置金额       │
│  3. 判断：                           │
│     ✅ 最大 >= 配置 → 执行取出       │
│     ⏭️ 最大 < 配置 → 跳过            │
│  4. 等待配置的间隔时间               │
│  5. 回到步骤1，继续循环              │
└─────────────────────────────────────┘
```

### 执行条件

只有当 **最大可取出金额 ≥ 配置的取出金额** 时，才会执行取出操作。

**示例：**
- 配置取出金额：`0.5`
- 当前最大可取出：`0.3` → ⏭️ **跳过**（等待下次）
- 当前最大可取出：`0.6` → ✅ **执行取出**（取出0.5）

## ⚙️ 配置检查

确保`.env`文件包含：

```bash
BSC_RPC_URL=https://bsc-dataseed1.binance.org
WALLET_PRIVATE_KEY=0x你的私钥
```

## 📝 参考交易

[0x3dc2f3c361df0e6d37b2b3deb188bbbf99211f10c6a621d98ffaa703b7ae5a2f](https://bscscan.com/tx/0x3dc2f3c361df0e6d37b2b3deb188bbbf99211f10c6a621d98ffaa703b7ae5a2f)

## 📊 输出示例

```
🚀 Lista MEV合约定期Withdraw工具
============================================================
⚙️ 配置信息:
   检查间隔: 60 秒
   取出金额: 0.5
   运行模式: 循环执行
============================================================

🔁 开始循环执行，每 60 秒检查一次
💡 按 Ctrl+C 停止程序
============================================================

============================================================
🔄 第 1 次检查 - 2025-11-06 15:30:00
============================================================
💎 最大可取出金额: 0.315223
⚙️ 配置取出金额: 0.500000
⚠️ 最大可取出金额 (0.315223) 小于配置金额 (0.500000)
⏭️ 跳过本次取出，等待下次检查

📊 统计信息:
   总检查次数: 1
   成功取出: 0
   跳过/失败: 1

⏰ 等待 60 秒后进行下次检查...
============================================================

... (等待60秒后继续下一次检查)
```

## 🛑 停止程序

按 `Ctrl + C` 停止循环执行

## ⚠️ 安全提醒

- 🔐 私钥不要泄露
- ✅ 确认地址和金额配置正确
- 💰 确保有足够的BNB支付gas
- ⏰ 合理设置检查间隔，避免频繁调用
- 📊 监控程序运行状态和日志输出

## 🔧 高级用法

### 后台运行

```bash
# 使用nohup在后台运行
nohup python lista_withdraw.py --amount 0.5 --interval 60 > withdraw.log 2>&1 &

# 查看日志
tail -f withdraw.log

# 停止后台进程
ps aux | grep lista_withdraw.py
kill <进程ID>
```

### 使用screen/tmux

```bash
# 创建新会话
screen -S lista_withdraw

# 运行程序
python lista_withdraw.py --amount 0.5 --interval 60

# 分离会话：按 Ctrl+A 然后按 D

# 重新连接
screen -r lista_withdraw
```
